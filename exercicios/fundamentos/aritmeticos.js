const [a, b, c, d] = [3, 5, 1, 15]

// ++a // Operador prefix - antes da utilização
// a++ // Operador posfix - após a utilização
// a + b // Operador in fix - sempre binário

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, -divisao, modulo)